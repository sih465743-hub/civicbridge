import { a as useCurrentUserState, i as RedirectToSignIn, t as Page } from "./shell-CtTFp-Fl.js";
import { c as listPartners, l as listPublicProblems, n as expressInterest, o as listMyInterests } from "./server-MU9Szium.js";
import { r as Button } from "./router-DOMSjLsP.js";
import { t as Badge } from "./badge-BIaEEku-.js";
import { t as RoleSwitcher } from "./role-switcher-Ba4MhAbg.js";
import { t as Input } from "./input-oyIDZsOG.js";
import { useCallback, useEffect, useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
//#region src/routes/university.tsx?tsr-split=component
function UniversityPage() {
	const { user, isPending } = useCurrentUserState();
	const [ready, setReady] = useState(false);
	const [problems, setProblems] = useState([]);
	const [partners, setPartners] = useState([]);
	const [org, setOrg] = useState("BIT Mesra");
	const [note, setNote] = useState("We can field a student team under NEP community engagement.");
	const [interests, setInterests] = useState([]);
	const load = useCallback(() => {
		Promise.all([
			listPublicProblems(),
			listPartners(),
			listMyInterests()
		]).then(([p, partners, mine]) => {
			setProblems(p);
			setPartners(partners);
			setInterests(mine.map((i) => ({
				id: i.id,
				title: i.title,
				status: i.status
			})));
		});
	}, []);
	useEffect(() => {
		if (ready) load();
	}, [ready, load]);
	if (isPending) return /* @__PURE__ */ jsx(Page, { children: /* @__PURE__ */ jsx("div", { className: "h-40 animate-pulse rounded-xl bg-border/60" }) });
	if (!user) return /* @__PURE__ */ jsx(RedirectToSignIn, {});
	async function claim(problemId) {
		await expressInterest({ data: {
			problemId,
			orgName: org,
			note
		} });
		load();
	}
	return /* @__PURE__ */ jsxs(Page, { children: [
		/* @__PURE__ */ jsx("h1", {
			className: "font-display text-4xl text-ink",
			children: "University workspace"
		}),
		/* @__PURE__ */ jsx("p", {
			className: "mt-2 max-w-2xl text-muted",
			children: "Discover master challenges that match departmental expertise. Interest is logged for admin review — a path toward NEP credits and later CSR pilots."
		}),
		/* @__PURE__ */ jsx("div", {
			className: "mt-6",
			children: /* @__PURE__ */ jsx(RoleSwitcher, {
				needed: "university",
				onReady: (p) => {
					setReady(p.role === "university" || p.role === "admin");
					if (p.org_name) setOrg(p.org_name);
				}
			})
		}),
		ready ? /* @__PURE__ */ jsxs(Fragment, { children: [
			/* @__PURE__ */ jsxs("div", {
				className: "mt-6 grid gap-3 sm:grid-cols-2",
				children: [/* @__PURE__ */ jsx(Input, {
					value: org,
					onChange: (e) => setOrg(e.target.value),
					placeholder: "Institution name"
				}), /* @__PURE__ */ jsx(Input, {
					value: note,
					onChange: (e) => setNote(e.target.value),
					placeholder: "Why this team"
				})]
			}),
			/* @__PURE__ */ jsx("h2", {
				className: "mt-10 font-display text-2xl",
				children: "Partner map"
			}),
			/* @__PURE__ */ jsx("div", {
				className: "mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-3",
				children: partners.map((p) => /* @__PURE__ */ jsxs("div", {
					className: "rounded-lg border border-border bg-surface p-4",
					children: [
						/* @__PURE__ */ jsx("div", {
							className: "text-sm font-medium",
							children: p.name
						}),
						/* @__PURE__ */ jsx("div", {
							className: "mt-1 text-xs capitalize text-muted",
							children: p.kind
						}),
						/* @__PURE__ */ jsx("div", {
							className: "mt-2 text-xs text-subtle",
							children: p.expertise
						})
					]
				}, p.id))
			}),
			/* @__PURE__ */ jsx("h2", {
				className: "mt-10 font-display text-2xl",
				children: "Open challenges"
			}),
			/* @__PURE__ */ jsx("div", {
				className: "mt-4 space-y-3",
				children: problems.filter((p) => p.status !== "resolved" && p.status !== "closed").map((p) => /* @__PURE__ */ jsxs("div", {
					className: "rounded-xl border border-border bg-surface p-5",
					children: [
						/* @__PURE__ */ jsxs("div", {
							className: "flex flex-wrap gap-2",
							children: [/* @__PURE__ */ jsx(Badge, { children: p.category }), /* @__PURE__ */ jsx(Badge, { children: p.required_expertise })]
						}),
						/* @__PURE__ */ jsx("h3", {
							className: "mt-2 font-display text-xl",
							children: p.title
						}),
						/* @__PURE__ */ jsx("p", {
							className: "mt-1 text-sm text-muted",
							children: p.summary
						}),
						/* @__PURE__ */ jsx(Button, {
							className: "mt-4",
							size: "sm",
							onClick: () => void claim(p.id),
							children: "Express interest"
						})
					]
				}, p.id))
			}),
			/* @__PURE__ */ jsx("h2", {
				className: "mt-10 font-display text-2xl",
				children: "Your interest log"
			}),
			/* @__PURE__ */ jsx("ul", {
				className: "mt-3 space-y-2",
				children: interests.map((i) => /* @__PURE__ */ jsxs("li", {
					className: "text-sm text-muted",
					children: [
						i.title,
						" · ",
						i.status
					]
				}, i.id))
			})
		] }) : null
	] });
}
//#endregion
export { UniversityPage as component };
