import { i as TSS_SERVER_FUNCTION, r as createServerFn } from "../index.js";
import { i as getSql, t as authMiddleware } from "./middleware-o8Z5luPe.js";
import { t as CATEGORIES } from "./categories-DxhXwv1q.js";
//#region node_modules/@tanstack/start-server-core/dist/esm/createServerRpc.js
var createServerRpc = (serverFnMeta, splitImportFn) => {
	const url = "/_serverFn/" + serverFnMeta.id;
	return Object.assign(splitImportFn, {
		url,
		serverFnMeta,
		[TSS_SERVER_FUNCTION]: true
	});
};
//#endregion
//#region src/lib/civic/dedup.ts
function tokens(text) {
	return new Set(text.toLowerCase().replace(/[^a-z0-9\s]/g, " ").split(/\s+/).filter((t) => t.length > 2));
}
function jaccard(a, b) {
	const A = tokens(a);
	const B = tokens(b);
	if (A.size === 0 || B.size === 0) return 0;
	let inter = 0;
	for (const t of A) if (B.has(t)) inter += 1;
	return inter / (A.size + B.size - inter);
}
function localityClose(a, b) {
	if (!a || !b) return true;
	const x = a.toLowerCase().trim();
	const y = b.toLowerCase().trim();
	return x === y || x.includes(y) || y.includes(x);
}
function fingerprint(parts) {
	return parts.filter(Boolean).join(" ").toLowerCase().replace(/[^a-z0-9\s]/g, " ").replace(/\s+/g, " ").trim();
}
//#endregion
//#region src/lib/civic/ai.ts
var KEYWORDS = [
	{
		category: "water",
		words: [
			"water",
			"handpump",
			"pump",
			"leak",
			"pipe",
			"tap",
			"well",
			"jal"
		]
	},
	{
		category: "sanitation",
		words: [
			"garbage",
			"waste",
			"drain",
			"sewage",
			"toilet",
			"dump"
		]
	},
	{
		category: "education",
		words: [
			"school",
			"teacher",
			"textbook",
			"classroom",
			"anganwadi",
			"primer"
		]
	},
	{
		category: "health",
		words: [
			"hospital",
			"clinic",
			"doctor",
			"health",
			"medicine"
		]
	},
	{
		category: "infrastructure",
		words: [
			"pothole",
			"road",
			"streetlight",
			"bridge",
			"light"
		]
	},
	{
		category: "energy",
		words: [
			"electricity",
			"power",
			"transformer",
			"outage"
		]
	},
	{
		category: "agriculture",
		words: [
			"crop",
			"farm",
			"irrigation",
			"kisan"
		]
	},
	{
		category: "environment",
		words: [
			"tree",
			"pollution",
			"forest",
			"smoke"
		]
	}
];
function heuristicStructure(text, locality, district) {
	const lower = text.toLowerCase();
	let category = "other";
	for (const row of KEYWORDS) if (row.words.some((w) => lower.includes(w))) {
		category = row.category;
		break;
	}
	const urgent = /no water|accident|collapse|overflow|days? without/.test(lower);
	const title = text.replace(/\s+/g, " ").trim().slice(0, 88);
	const summary = text.replace(/\s+/g, " ").trim().slice(0, 280);
	return {
		title: title || "Civic report",
		summary: summary || title,
		category,
		subcategory: category,
		priority: urgent ? "high" : "medium",
		locality: locality || null,
		district: district || null,
		entities: [],
		requiredExpertise: category,
		safetyFlags: urgent ? ["needs-attention"] : [],
		fingerprint: fingerprint([
			title,
			category,
			locality,
			district
		]),
		usedAi: false
	};
}
async function structureReport(input) {
	const fallback = heuristicStructure(input.text, input.locality, input.district);
	const apiKey = process.env.XAI_API_KEY;
	if (!apiKey) return fallback;
	try {
		const res = await fetch("https://api.x.ai/v1/chat/completions", {
			method: "POST",
			headers: {
				"Content-Type": "application/json",
				Authorization: `Bearer ${apiKey}`
			},
			body: JSON.stringify({
				model: "grok-4.5",
				max_tokens: 500,
				temperature: .2,
				messages: [{
					role: "system",
					content: "You extract civic problem facts from citizen reports in Jharkhand, India. Reply with JSON only. No markdown."
				}, {
					role: "user",
					content: `Report: ${input.text}
Locality hint: ${input.locality ?? ""}
District hint: ${input.district ?? ""}
Return JSON keys: title, summary, category (one of ${CATEGORIES.join(",")}), subcategory, priority (low|medium|high|urgent), locality, district, entities (string[]), requiredExpertise, safetyFlags (string[]), fingerprint.`
				}]
			})
		});
		if (!res.ok) return fallback;
		const jsonText = ((await res.json()).choices?.[0]?.message?.content ?? "").replace(/```json|```/g, "").trim();
		const parsed = JSON.parse(jsonText);
		const category = CATEGORIES.includes(parsed.category) ? parsed.category : fallback.category;
		const title = String(parsed.title || fallback.title).slice(0, 120);
		return {
			title,
			summary: String(parsed.summary || fallback.summary).slice(0, 400),
			category,
			subcategory: String(parsed.subcategory || category),
			priority: parsed.priority || fallback.priority,
			locality: parsed.locality || input.locality || null,
			district: parsed.district || input.district || null,
			entities: Array.isArray(parsed.entities) ? parsed.entities.map(String).slice(0, 8) : [],
			requiredExpertise: String(parsed.requiredExpertise || category),
			safetyFlags: Array.isArray(parsed.safetyFlags) ? parsed.safetyFlags.map(String).slice(0, 6) : [],
			fingerprint: fingerprint([
				parsed.fingerprint || title,
				category,
				parsed.locality,
				parsed.district
			]),
			usedAi: true
		};
	} catch {
		return fallback;
	}
}
//#endregion
//#region src/lib/civic/server.ts?tss-serverfn-split
function nid(prefix) {
	return `${prefix}_${crypto.randomUUID().slice(0, 10)}`;
}
async function ensureProfile(userId, displayName) {
	await (await getSql())`
    insert into profiles (user_id, display_name)
    values (${userId}, ${displayName ?? null})
    on conflict (user_id) do nothing
  `;
}
var getMyProfile_createServerFn_handler = createServerRpc({
	id: "7d49ff966589d1371e556eee908408688fe4aa1a68716000ff7c6ef5abb2387f",
	name: "getMyProfile",
	filename: "src/lib/civic/server.ts"
}, (opts) => getMyProfile.__executeServer(opts));
var getMyProfile = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(getMyProfile_createServerFn_handler, async ({ context }) => {
	await ensureProfile(context.userId);
	return (await (await getSql())`
      select user_id, display_name, role, org_name, expertise
      from profiles where user_id = ${context.userId}
    `)[0] ?? null;
});
var setMyRole_createServerFn_handler = createServerRpc({
	id: "02fb2829befb2ff4cc69bf3bb55fca52a451eb6e13395fc1956e14ab2e924ce3",
	name: "setMyRole",
	filename: "src/lib/civic/server.ts"
}, (opts) => setMyRole.__executeServer(opts));
var setMyRole = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((input) => input).handler(setMyRole_createServerFn_handler, async ({ context, data }) => {
	if (![
		"citizen",
		"officer",
		"university",
		"admin"
	].includes(data.role)) throw new Error("Invalid role");
	await ensureProfile(context.userId);
	const sql = await getSql();
	await sql`
      update profiles
      set role = ${data.role},
          org_name = coalesce(${data.orgName ?? null}, org_name),
          expertise = coalesce(${data.expertise ?? null}, expertise)
      where user_id = ${context.userId}
    `;
	await sql`
      insert into audit_logs (id, user_id, action, detail)
      values (${nid("a")}, ${context.userId}, 'role.set', ${data.role})
    `;
	return { ok: true };
});
var listPublicProblems_createServerFn_handler = createServerRpc({
	id: "d36346cf390584b925f002f63f6e5ba49781fcc2470cb79e7fdd00f2416532e1",
	name: "listPublicProblems",
	filename: "src/lib/civic/server.ts"
}, (opts) => listPublicProblems.__executeServer(opts));
var listPublicProblems = createServerFn({ method: "GET" }).handler(listPublicProblems_createServerFn_handler, async () => {
	return (await getSql())`
    select id, title, summary, category, subcategory, priority, locality, district,
           lat, lng, status, report_count, required_expertise, fingerprint, confidence,
           created_at::text as created_at, updated_at::text as updated_at
    from problems
    order by report_count desc, updated_at desc
  `;
});
var getPublicProblem_createServerFn_handler = createServerRpc({
	id: "20ec21687ba8516465b44d4e1436197a7bfcfe94c2f7a31605442ffa98021861",
	name: "getPublicProblem",
	filename: "src/lib/civic/server.ts"
}, (opts) => getPublicProblem.__executeServer(opts));
var getPublicProblem = createServerFn({ method: "GET" }).validator((input) => input).handler(getPublicProblem_createServerFn_handler, async ({ data }) => {
	const sql = await getSql();
	const problem = (await sql`
      select id, title, summary, category, subcategory, priority, locality, district,
             lat, lng, status, report_count, required_expertise, fingerprint, confidence,
             created_at::text as created_at, updated_at::text as updated_at
      from problems where id = ${data.id}
    `)[0];
	if (!problem) return null;
	const events = await sql`
      select id, problem_id, actor_user_id, kind, note, created_at::text as created_at
      from status_events where problem_id = ${data.id}
      order by created_at asc
    `;
	const evidence = await sql`
      select count(*)::int as count from reports where problem_id = ${data.id}
    `;
	const interests = await sql`
      select count(*)::int as count from university_interests where problem_id = ${data.id}
    `;
	return {
		problem,
		events,
		evidenceCount: evidence[0]?.count ?? 0,
		interestCount: interests[0]?.count ?? 0
	};
});
var listPartners_createServerFn_handler = createServerRpc({
	id: "e35930a98279ae7ba7d6387b72a4c51ba207690bf51da453d4542f5b19bee77a",
	name: "listPartners",
	filename: "src/lib/civic/server.ts"
}, (opts) => listPartners.__executeServer(opts));
var listPartners = createServerFn({ method: "GET" }).handler(listPartners_createServerFn_handler, async () => {
	return (await getSql())`select id, name, kind, expertise, district, capacity from partners order by name`;
});
var listMyReports_createServerFn_handler = createServerRpc({
	id: "adea2daef3c6b753e6ee76e85104ff39373322090e3f719faf8f47b3912e214d",
	name: "listMyReports",
	filename: "src/lib/civic/server.ts"
}, (opts) => listMyReports.__executeServer(opts));
var listMyReports = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(listMyReports_createServerFn_handler, async ({ context }) => {
	return (await getSql())`
      select id, user_id, problem_id, source, raw_text, structured_title, structured_summary,
             category, locality, district, processing_status, relationship_type,
             relationship_confidence, created_at::text as created_at
      from reports
      where user_id = ${context.userId}
      order by created_at desc
    `;
});
var submitCivicReport_createServerFn_handler = createServerRpc({
	id: "90be212e6f8e88c5a374ea65691aeda1b9137670f4353b18bcaa0b5b8495d090",
	name: "submitCivicReport",
	filename: "src/lib/civic/server.ts"
}, (opts) => submitCivicReport.__executeServer(opts));
var submitCivicReport = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((input) => input).handler(submitCivicReport_createServerFn_handler, async ({ context, data }) => {
	const text = data.text.trim();
	if (text.length < 8) throw new Error("Please describe the problem in a bit more detail.");
	await ensureProfile(context.userId);
	const sql = await getSql();
	const structured = await structureReport({
		text,
		locality: data.locality,
		district: data.district
	});
	const reportId = nid("r");
	await sql`
      insert into reports (
        id, user_id, source, raw_text, structured_title, structured_summary,
        category, locality, district, processing_status
      ) values (
        ${reportId}, ${context.userId}, ${data.source ?? "web"}, ${text},
        ${structured.title}, ${structured.summary}, ${structured.category},
        ${structured.locality}, ${structured.district}, 'processing'
      )
    `;
	const candidates = await sql`
      select id, title, summary, category, subcategory, priority, locality, district,
             lat, lng, status, report_count, required_expertise, fingerprint, confidence,
             created_at::text as created_at, updated_at::text as updated_at
      from problems
      where status <> 'closed'
      order by updated_at desc
      limit 80
    `;
	let best = null;
	const query = `${structured.fingerprint} ${structured.title} ${structured.summary}`;
	for (const p of candidates) {
		if (p.category !== structured.category) continue;
		if (!localityClose(p.locality, structured.locality) && p.district !== structured.district) continue;
		const score = Math.max(jaccard(query, `${p.fingerprint} ${p.title} ${p.summary}`), jaccard(structured.title, p.title));
		if (!best || score > best.score) best = {
			problem: p,
			score
		};
	}
	const merge = best && best.score >= .28;
	let problemId;
	let relationship = "new";
	if (merge && best) {
		problemId = best.problem.id;
		relationship = "same";
		await sql`
        update problems
        set report_count = report_count + 1,
            updated_at = now(),
            confidence = greatest(confidence, ${best.score})
        where id = ${problemId}
      `;
		await sql`
        insert into status_events (id, problem_id, actor_user_id, kind, note)
        values (
          ${nid("e")}, ${problemId}, ${context.userId}, 'reports.consolidated',
          ${`Citizen report merged (score ${best.score.toFixed(2)}).`}
        )
      `;
	} else {
		problemId = nid("pr");
		await sql`
        insert into problems (
          id, title, summary, category, subcategory, priority, locality, district,
          status, report_count, required_expertise, fingerprint, confidence
        ) values (
          ${problemId}, ${structured.title}, ${structured.summary}, ${structured.category},
          ${structured.subcategory}, ${structured.priority}, ${structured.locality},
          ${structured.district}, 'open', 1, ${structured.requiredExpertise},
          ${structured.fingerprint}, ${structured.usedAi ? .82 : .62}
        )
      `;
		await sql`
        insert into status_events (id, problem_id, actor_user_id, kind, note)
        values (
          ${nid("e")}, ${problemId}, ${context.userId}, 'problem.created',
          ${structured.usedAi ? "AI structured a new master challenge." : "Heuristic structured a new master challenge."}
        )
      `;
	}
	await sql`
      update reports
      set problem_id = ${problemId},
          processing_status = 'processed',
          relationship_type = ${relationship},
          relationship_confidence = ${merge && best ? best.score : 1}
      where id = ${reportId} and user_id = ${context.userId}
    `;
	const problems = await sql`
      select id, title, summary, category, subcategory, priority, locality, district,
             lat, lng, status, report_count, required_expertise, fingerprint, confidence,
             created_at::text as created_at, updated_at::text as updated_at
      from problems where id = ${problemId}
    `;
	return {
		reportId,
		relationship,
		usedAi: structured.usedAi,
		structured,
		problem: problems[0],
		mergeScore: merge && best ? best.score : null
	};
});
var updateProblemStatus_createServerFn_handler = createServerRpc({
	id: "c6895b88d3d014a2f4dcdcc28e0c51f2996e2f072ed9ad70864b5f4cad0ba3be",
	name: "updateProblemStatus",
	filename: "src/lib/civic/server.ts"
}, (opts) => updateProblemStatus.__executeServer(opts));
var updateProblemStatus = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((input) => input).handler(updateProblemStatus_createServerFn_handler, async ({ context, data }) => {
	const sql = await getSql();
	const role = (await sql`
      select user_id, display_name, role, org_name, expertise
      from profiles where user_id = ${context.userId}
    `)[0]?.role ?? "citizen";
	if (role !== "officer" && role !== "admin") throw new Error("Officer workspace required.");
	await sql`
      update problems set status = ${data.status}, updated_at = now()
      where id = ${data.problemId}
    `;
	await sql`
      insert into status_events (id, problem_id, actor_user_id, kind, note)
      values (${nid("e")}, ${data.problemId}, ${context.userId}, ${data.status}, ${data.note ?? null})
    `;
	await sql`
      insert into audit_logs (id, user_id, action, detail)
      values (${nid("a")}, ${context.userId}, 'problem.status', ${`${data.problemId}:${data.status}`})
    `;
	return { ok: true };
});
var expressInterest_createServerFn_handler = createServerRpc({
	id: "4c3846d2488f8432a5ba18eb1a82de3ecdd35dbceba29d606fda82a15e0c0d53",
	name: "expressInterest",
	filename: "src/lib/civic/server.ts"
}, (opts) => expressInterest.__executeServer(opts));
var expressInterest = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((input) => input).handler(expressInterest_createServerFn_handler, async ({ context, data }) => {
	if (!data.orgName.trim()) throw new Error("Organisation name is required.");
	await ensureProfile(context.userId);
	const sql = await getSql();
	const id = nid("i");
	await sql`
      insert into university_interests (id, user_id, problem_id, org_name, note)
      values (${id}, ${context.userId}, ${data.problemId}, ${data.orgName.trim()}, ${data.note ?? null})
    `;
	await sql`
      insert into status_events (id, problem_id, actor_user_id, kind, note)
      values (${nid("e")}, ${data.problemId}, ${context.userId}, 'university.interest', ${data.orgName.trim()})
    `;
	return { id };
});
var listMyInterests_createServerFn_handler = createServerRpc({
	id: "450fd454390bc067df3918bfcaaaf8a35b0580058df3728eb868b3ef459c5afe",
	name: "listMyInterests",
	filename: "src/lib/civic/server.ts"
}, (opts) => listMyInterests.__executeServer(opts));
var listMyInterests = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(listMyInterests_createServerFn_handler, async ({ context }) => {
	return (await getSql())`
      select i.id, i.user_id, i.problem_id, i.org_name, i.note, i.status,
             i.created_at::text as created_at, p.title
      from university_interests i
      join problems p on p.id = i.problem_id
      where i.user_id = ${context.userId}
      order by i.created_at desc
    `;
});
var listAllInterests_createServerFn_handler = createServerRpc({
	id: "880297a30181b56ddfffbe9a2cf59fe4e5cdc74d7f8f5586e47fb2663c208e36",
	name: "listAllInterests",
	filename: "src/lib/civic/server.ts"
}, (opts) => listAllInterests.__executeServer(opts));
var listAllInterests = createServerFn({ method: "GET" }).middleware([authMiddleware]).handler(listAllInterests_createServerFn_handler, async ({ context }) => {
	const sql = await getSql();
	if ((await sql`
      select user_id, display_name, role, org_name, expertise
      from profiles where user_id = ${context.userId}
    `)[0]?.role !== "admin") throw new Error("Admin workspace required.");
	return sql`
      select i.id, i.user_id, i.problem_id, i.org_name, i.note, i.status,
             i.created_at::text as created_at, p.title
      from university_interests i
      join problems p on p.id = i.problem_id
      order by i.created_at desc
    `;
});
var reviewInterest_createServerFn_handler = createServerRpc({
	id: "1e8cd0e0286bde136c060ad4eb5d827e4805699f10a7752ce8bc28a13ca9f40a",
	name: "reviewInterest",
	filename: "src/lib/civic/server.ts"
}, (opts) => reviewInterest.__executeServer(opts));
var reviewInterest = createServerFn({ method: "POST" }).middleware([authMiddleware]).validator((input) => input).handler(reviewInterest_createServerFn_handler, async ({ context, data }) => {
	const sql = await getSql();
	if ((await sql`
      select user_id, display_name, role, org_name, expertise
      from profiles where user_id = ${context.userId}
    `)[0]?.role !== "admin") throw new Error("Admin workspace required.");
	await sql`
      update university_interests set status = ${data.status} where id = ${data.id}
    `;
	return { ok: true };
});
var dashboardStats_createServerFn_handler = createServerRpc({
	id: "8812813a7aa6f602ff1d5893d59c530847bc57fb56c1bbfcfdade90fe4ad8874",
	name: "dashboardStats",
	filename: "src/lib/civic/server.ts"
}, (opts) => dashboardStats.__executeServer(opts));
var dashboardStats = createServerFn({ method: "GET" }).handler(dashboardStats_createServerFn_handler, async () => {
	const sql = await getSql();
	const problems = await sql`select count(*)::int as count from problems`;
	const reports = await sql`select count(*)::int as count from reports`;
	const open = await sql`select count(*)::int as count from problems where status = 'open'`;
	const merged = await sql`
    select coalesce(sum(report_count - 1), 0)::int as count from problems where report_count > 1
  `;
	const citizens = await sql`select coalesce(sum(report_count), 0)::int as count from problems`;
	return {
		problems: problems[0]?.count ?? 0,
		reports: reports[0]?.count ?? 0,
		open: open[0]?.count ?? 0,
		duplicatesAbsorbed: merged[0]?.count ?? 0,
		citizensRepresented: citizens[0]?.count ?? 0
	};
});
//#endregion
export { dashboardStats_createServerFn_handler, expressInterest_createServerFn_handler, getMyProfile_createServerFn_handler, getPublicProblem_createServerFn_handler, listAllInterests_createServerFn_handler, listMyInterests_createServerFn_handler, listMyReports_createServerFn_handler, listPartners_createServerFn_handler, listPublicProblems_createServerFn_handler, reviewInterest_createServerFn_handler, setMyRole_createServerFn_handler, submitCivicReport_createServerFn_handler, updateProblemStatus_createServerFn_handler };
